= render partial: 'records/field',
         locals: { field: :format }

= render partial: 'records/field',
         locals: { field: :kytopic, field_label: "KY Topic" }

= render partial: 'records/field',
         collection: [:languages, :type, :title, :creator, :date, :archival_container_format, :subjects, :repository]

= link_to 'Edit', edit_record_path(@record) 
= link_to 'Back', records_path
