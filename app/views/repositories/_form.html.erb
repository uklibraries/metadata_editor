<%= simple_form_for(@repository) do |f| %>
  <% [
       :name,
       :email,
       :url,
       :street_address,
       :abstract,
     ].each do |field|
     %>
    <%= f.input field, tooltip: I18n.t("repositories.#{field}") %>
  <% end %>

  <%= f.button :submit %>
<% end %>
